# Slack Socket Mode (orchestrator app)
# Bot token (xoxb-)
SLACK_BOT_TOKEN=
# App-level token for Socket Mode (xapp-)
SLACK_APP_TOKEN=

# Squad: either explicit alex=Uxxx,tim=Uyyy,... or comma-separated Slack user IDs in default order
# (alex, tim, ross, garth, joanne) — same idea as employee-factory. Shuffle seed is derived from the map.
MULTIAGENT_BOT_USER_IDS=

# Optional: override shuffle/plain-message seed (advanced). If unset, derived from MULTIAGENT_BOT_USER_IDS.
# MULTIAGENT_SHUFFLE_SECRET=

# Optional: comma-separated keys to pin order (overrides shuffle). Leave unset for normal operation.
# MULTIAGENT_ORDER=

# @everyone uses first N in the resolved roster; @channel uses first N (limits below)
EVERYONE_AGENT_LIMIT=5
CHANNEL_AGENT_LIMIT=3

HTTP_ADDR=:8080
# JSON logs default on; set LOG_JSON=false to disable

# --- Phase 2: HTTP dispatch to employee-factory workers (optional) ---
# ORCHESTRATOR_DISPATCH_ENABLED=false
# Base URL per employee; "{employee}" is replaced (e.g. alex → http://employee-factory-alex.employee-factory.svc.cluster.local:8080)
# ORCHESTRATOR_WORKER_URL_TEMPLATE=http://employee-factory-{employee}.employee-factory.svc.cluster.local:8080
# Shared secret: HMAC-SHA256 body, header X-BimRoss-Orchestrator-Signature: v1=<hex> (must match worker ORCHESTRATOR_INGRESS_HMAC_SECRET)
# ORCHESTRATOR_WORKER_HMAC_SECRET=
# ORCHESTRATOR_DISPATCH_TIMEOUT_SEC=10
